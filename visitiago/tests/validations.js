export const verifyFullName = (fullname) => {
	if (fullname === ''){
		return false;
	} else
		return true;
  }

  export const verifyRUT = (rut) => {
	if (rut === ''){
		return false;
	} else
		return true;
  }